############################################################
# Runtime dependencies for synced_ereader_api
# This file mirrors the [project.dependencies] in
# synced_ereader_api/pyproject.toml so that environments or
# platforms expecting a requirements.txt can still install.
# Edit pyproject.toml first, then reflect changes here.
############################################################

Flask>=3.0.2,<4
openai-whisper
python-vlc
pydub
pynput

# Dev / lint / test tools are declared as the 'dev' optional
# dependency group in pyproject.toml and intentionally not
# duplicated here. Install them with:
#   pip install -e .[dev]